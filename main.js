function toggleMenu() {
  const navLinks = document.getElementById("navLinks");
  navLinks.style.display = navLinks.style.display === "flex" ? "none" : "flex";
}

// Example: simple alert when page loads
window.onload = () => {
  console.log("Welcome to Surath Manandhar's Portfolio!");
};
